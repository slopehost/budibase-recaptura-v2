import Redlock, { Options } from "redlock";
import { LockOptions } from "@budibase/types";
export declare const OPTIONS: {
    TRY_ONCE: {
        retryCount: number;
    };
    DEFAULT: {
        driftFactor: number;
        retryCount: number;
        retryDelay: number;
        retryJitter: number;
    };
    DELAY_500: {
        retryDelay: number;
    };
};
export declare const newRedlock: (opts?: Options) => Promise<Redlock>;
export declare const doWithLock: (opts: LockOptions, task: any) => Promise<any>;
