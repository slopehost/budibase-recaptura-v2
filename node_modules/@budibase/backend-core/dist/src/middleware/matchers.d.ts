import { BBContext, EndpointMatcher, RegexMatcher } from "@budibase/types";
export declare const buildMatcherRegex: (patterns: EndpointMatcher[]) => RegexMatcher[];
export declare const matches: (ctx: BBContext, options: RegexMatcher[]) => RegexMatcher | undefined;
