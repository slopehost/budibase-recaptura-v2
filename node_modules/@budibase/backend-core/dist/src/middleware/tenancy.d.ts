import { BBContext, EndpointMatcher } from "@budibase/types";
declare const _default: (allowQueryStringPatterns: EndpointMatcher[], noTenancyPatterns: EndpointMatcher[], opts?: {
    noTenancyRequired?: boolean;
}) => (ctx: BBContext | any, next: any) => Promise<any>;
export = _default;
