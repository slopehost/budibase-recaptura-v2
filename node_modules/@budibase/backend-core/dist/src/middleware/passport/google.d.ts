import { SaveUserFunction } from "./third-party-common";
import { GoogleConfig, Database, SSOProfile } from "@budibase/types";
export declare function buildVerifyFn(saveUserFn?: SaveUserFunction): (accessToken: string, refreshToken: string, profile: SSOProfile, done: Function) => Promise<any>;
/**
 * Create an instance of the google passport strategy. This wrapper fetches the configuration
 * from couchDB rather than environment variables, using this factory is necessary for dynamically configuring passport.
 * @returns Dynamically configured Passport Google Strategy
 */
export declare function strategyFactory(config: GoogleConfig["config"], callbackUrl: string, saveUserFn?: SaveUserFunction): Promise<any>;
export declare function getCallbackUrl(db: Database, config: {
    callbackURL?: string;
}): Promise<string>;
