import { ThirdPartyUser } from "@budibase/types";
declare type SaveUserOpts = {
    requirePassword?: boolean;
    hashPassword?: boolean;
    currentUserId?: string;
};
export declare type SaveUserFunction = (user: ThirdPartyUser, opts: SaveUserOpts) => Promise<any>;
/**
 * Common authentication logic for third parties. e.g. OAuth, OIDC.
 */
export declare function authenticateThirdParty(thirdPartyUser: ThirdPartyUser, requireLocalAccount: boolean | undefined, done: Function, saveUserFn?: SaveUserFunction): Promise<any>;
export {};
